def insert_sort(arr):
    for i in range(1, len(arr)):
        j = i
        while j > 0 and arr[j] < arr[j - 1]:
            arr[j], arr[j - 1] = arr[j - 1], arr[j]
    return arr


print(insert_sort([4, 3, -1, 0, 2, 4, 1, 3, 2, -1, -2]))
